apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-clinic-frontend
  labels:
    app: smart-clinic-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smart-clinic-frontend
  template:
    metadata:
      labels:
        app: smart-clinic-frontend
    spec:
      containers:
      - name: frontend
        image: smart-clinic-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: smart-clinic-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
